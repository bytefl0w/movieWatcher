#include "database.h"
#include "fileio.h"

struct Movie{
  char* tconst, titleType, primaryTitle;
  char* originalTitle, isAdult, genres;
  int startYear, endYear, runtimeMinutes;
};

struct Movie * createDatabase(){
    int num;
    struct Movie *mov;
    return mov;
}